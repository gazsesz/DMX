gradle-wrapper.jar
/.gradle
/captures/
/gradlew
/gradlew.bat
/local.properties
GeneratedPluginRegistrant.java
.cxx/

# Remember to never publicly share your keystore.
# See https://flutter.dev/to/reference-keystore
key.properties
**/*.keystore
**/*.jks
# Exception: a fixed *debug*-only key (not a release-signing key) so every
# build — local or CI — signs identically and `adb install` can upgrade an
# existing install instead of refusing on a signature mismatch.
!app/debug.keystore
